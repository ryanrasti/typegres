#!/usr/bin/env bash
set -euo pipefail

ROOT="$(cd "$(dirname "$0")/.." && pwd)"
HASH="$(printf '%s' "$ROOT" | sha256sum | cut -c1-12)"
PGDATA="/tmp/typegres-pg-$HASH/data"

echo "Stopping PostgreSQL (data: $PGDATA)"
exec pg_ctl -D "$PGDATA" stop -m fast -w
