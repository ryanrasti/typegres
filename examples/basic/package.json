{
  "name": "typegres-example-basic",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "migrate": "node --experimental-strip-types migrate.ts",
    "generate": "tg generate",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "pg": "^8.16.0",
    "typegres": "file:../.."
  },
  "devDependencies": {
    "@types/pg": "^8.15.4",
    "vitest": "^4.1.4"
  }
}
